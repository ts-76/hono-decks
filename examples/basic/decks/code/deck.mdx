---
title: Code Verification
description: Shikiで強調表示したコードを、固定16:9キャンバスで読みやすく見せる
author: ts-76
date: 2026-07-17
tags: [code, shiki, mdx]
transition: fade
transitionDuration: 280ms
transitionEasing: cubic-bezier(.22, 1, .36, 1)
---

---
title: Fenced code
layout: code
notes: |
  HTMLに見えるJSXも安全にescapeされ、長いコードはキャンバスを壊さずスクロールする。
---

<div class="code-heading">
  <p>Syntax highlighting / fixed canvas</p>
  # Code verification
</div>

```ts
const view = <Slide title="Hello" />

export function labels(items: Array<{ id: string }>) {
  return items.map((item) => item.id).join(", ")
}
```

<p class="code-caption">Whitespace preserved. JSX escaped. Overflow contained.</p>

---
title: CodeBlock component
layout: code
notes: |
  filenameと行ハイライトをMDXの近くで宣言できることを示す。
---

<div class="code-heading compact">
  <p>Component API</p>
  ## CodeBlock component
</div>

<CodeBlock lang="ts" filename="worker.ts" highlight="2">
const app = new Hono()
app.get("/", (c) => c.text("ok"))
</CodeBlock>

<div class="code-proof">
  <span>filename</span>
  <span>highlight</span>
  <span>build-time Shiki</span>
</div>
