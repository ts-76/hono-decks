{
  "name": "@hono-slides/example-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "bun run typecheck && bun run test"
  },
  "dependencies": {
    "hono-slides": "workspace:*"
  }
}
